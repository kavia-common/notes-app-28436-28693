# Backend API - Example Environment Variables

# Security
SECRET_KEY=dev-secret-change-me
ACCESS_TOKEN_EXPIRE_MINUTES=500
ALGORITHM=HS256

# Database
# For local preview we align with DatabaseContainer running at localhost:5001
DATABASE_URL=postgresql+asyncpg://appuser:dbuser123@localhost:5001/myapp

# CORS
# Allow the WebFrontendContainer dev server
CORS_ALLOW_ORIGINS=http://localhost:3000

# AI Summarization service (local interface)
SUMMARIZATION_API_URL=http://localhost:3001
SUMMARIZATION_API_TOKEN=dev-ai-token

# Preview/development flags
PREVIEW_NO_AUTH=true
LOG_LEVEL=info
ENV=development
